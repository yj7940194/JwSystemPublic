# Copy to `.env` to override docker-compose defaults (do not commit `.env`).
#
# Docker image registry (default: docker.io). If Docker Hub is unreachable in your network
# (e.g. "failed to fetch oauth token" / auth.docker.io timeout), set this to a mirror/proxy
# registry domain that works for you, for example:
#   DOCKER_REGISTRY=docker.m.daocloud.io
DOCKER_REGISTRY=docker.io

# MySQL (only used when creating the data volume for the first time)
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=jw_springboot
MYSQL_USER=jw_user
MYSQL_PASSWORD=jw_password

# Backend: bypass captcha validation (recommended only for local dev/CI)
JW_CAPTCHA_BYPASS=false

# Host port mappings
BACKEND_PORT=8080
FRONTEND_PORT=8081

# Optional: remote host access (browser-side)
# VUE_APP_BASE_API=http://127.0.0.1:8080
# DEV_SERVER_PUBLIC=127.0.0.1:8081
